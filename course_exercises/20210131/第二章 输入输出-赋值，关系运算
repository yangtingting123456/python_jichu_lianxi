                                输入输出
输入：input(),通过键盘输入都是字符串，想得到其它类型，必须进行转换，按enter键表示输入结束
     input里面可以加入提示语句。
输出：print(),可以加入提示语句或直接输入某个值，也可以对某个结果进行转换或格式化，有多个输出用,隔开即可
     默认结束时，是有换行符，如果不想换行则用都好隔开，则在上一个输出语句，则加入end=‘,’
eg：print(i,end= '\t')  #\t制表符，键盘的tab键

                                赋值

                                关系运算符